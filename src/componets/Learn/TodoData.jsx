const TodoData = (props) => {
    const { name, age, aim } = props;

    return (<div className="todo-data">
        <div>Learning React</div>
        <div>Learning React</div>
    </div>);
}

export default TodoData;